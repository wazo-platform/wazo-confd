#!/usr/bin/make -f
# -*- makefile -*-

export PYBUILD_NAME=wazo-confd
export PYBUILD_DISABLE=test

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_installsystemd:
	dh_installsystemd --no-enable --no-start --name wazo-confd-worker@
	dh_installsystemd
