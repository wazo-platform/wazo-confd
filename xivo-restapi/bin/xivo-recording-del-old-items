#!/bin/sh
BASEDIR=$(dirname $0)
cd /var/lib/pf-xivo/sounds/campagnes
find -mtime +31 -name '*.wav' -exec $BASEDIR/xivo-recording-log-and-del {} +
su - -c "psql asterisk postgres -c \"delete from recording where end_time <= (current_timestamp - interval '31 days');\"" postgres